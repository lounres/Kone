---
title: Intersecting segments
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';
import {koneVersion} from '@site/inputData'


In this tutorial we will use [Bentley–Ottmann algorithm](https://en.wikipedia.org/wiki/Bentley%E2%80%93Ottmann_algorithm) that is already implemented in Kone to get all pairwise intersections of random segments.

<!-- truncate -->

In that tutorial you will use the following libraries from the Kone project.
<Tabs groupId="buildSystem">
    <TabItem value="gradle-kotlin" label="Gradle Kotlin DSL" default>
        <CodeBlock language="kotlin" title="build.gradle.kts" showLineNumbers>
            {
                `dependencies {
                |    // highlight-next-line
                |    implementation("dev.lounres:kone.algebraic:${koneVersion}")
                |}`.replaceAll(/ *\|/g, "")
            }
        </CodeBlock>
    </TabItem>
    <TabItem value="gradle-groovy" label="Gradle Groovy DSL">
        <CodeBlock language="groovy" title="build.gradle" showLineNumbers>
            {
                `dependencies {
                |    // highlight-next-line
                |    implementation 'dev.lounres:kone.algebraic:${koneVersion}'
                |}`.replaceAll(/ *\|/g, "")
            }
        </CodeBlock>
    </TabItem>
    <TabItem value="maven" label="Maven" default>
        <CodeBlock language="xml" title="pom.xml" showLineNumbers>
        {
            `<dependency>
            |    <!-- highlight-start -->
            |    <groupId>dev.lounres</groupId>
            |    <artifactId>kone.algebraic</artifactId>
            |    <version>${koneVersion}</version>
            |    <!-- highlight-end -->
            |</dependency>`.replaceAll(/ *\|/g, "")
        }
        </CodeBlock>
    </TabItem>
</Tabs>
It's enough to set up any simple Kotlin Gradle project and add dependency on the libraries above.